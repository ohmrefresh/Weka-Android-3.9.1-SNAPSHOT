apply plugin: 'com.android.library'

android {
  compileSdkVersion 23
  buildToolsVersion "23.0.2"

  defaultConfig {
    minSdkVersion 23
    targetSdkVersion 23
  }
}

dependencies {
  compile files('libs/java-cup.jar')
  compile files('libs/openbeans-1.0.jar')
  compile files('libs/mtj.jar')
}
